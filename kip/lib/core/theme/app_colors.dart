import 'package:flutter/material.dart';

class AppColors {
  // Evita que a classe seja instanciada
  AppColors._();

  // Cores do Degradê Principal
  static const Color gradientEnd = Color(0xFFE30613);
  static const Color gradientStart = Color(0xFF790F11);

  // Cores de Texto
  static const Color textLight = Colors.white;
  static const Color textDark = Colors.black;
  static const Color hintText = Color(0xFF868E98);

  // Cores dos Botões
  static const Color buttonPrimary = Color(0xFF44BC7C); // Verde do botão "Aceitar"
  static const Color buttonSecondary = Colors.transparent;

  // Outras cores
  static const Color white = Colors.white;
  static const Color lightGrey = Color(0xFFF3F6FB);
}